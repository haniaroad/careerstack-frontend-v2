import { Button as UiButton, buttonVariants } from '@/components/ui/button'

/** CareerStack Button — import this (not `@/components/ui/button`) in product UI. */
export const Button = UiButton
export { buttonVariants }
