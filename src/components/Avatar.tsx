import {
  Avatar as UiAvatar,
  AvatarFallback,
  AvatarImage,
} from '@/components/ui/avatar'

/** CareerStack Avatar — import these (not `@/components/ui/avatar`) in product UI. */
export const Avatar = UiAvatar
export { AvatarFallback, AvatarImage }
