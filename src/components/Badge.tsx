import { Badge as UiBadge, badgeVariants } from '@/components/ui/badge'

/** CareerStack Badge — import this (not `@/components/ui/badge`) in product UI. */
export const Badge = UiBadge
export { badgeVariants }
