services:
  web:
    build:
      context: .
      args:
        VITE_API_BASE_URL: http://localhost:3000
    ports:
      - "5173:80"
    # Pair with backend Compose (`api` on :3000). CORS must allow http://localhost:5173.
